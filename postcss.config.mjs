export default {
    plugins: {
        // Tailwind v4 is handled by the @tailwindcss/vite plugin in vite.config.ts
        // This local config prevents Vite from searching upwards to C:\postcss.config.mjs
    },
}
